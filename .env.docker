# BTD Payment Service Docker Environment Variables
# Last Updated On: 2025-08-06
# This file is used when running the service in Docker

# Application
NODE_ENV=development
PORT=3500

# Database (using Docker network)
DATABASE_URL=postgresql://btd_user:btd_local_password@postgres:5432/btd_payments

# Redis (using Docker network)
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_DB=0

# Stripe Configuration
# These are inherited from the root .env file via docker-compose
# STRIPE_SECRET_KEY=${STRIPE_SECRET_KEY}
# STRIPE_PUBLISHABLE_KEY=${STRIPE_PUBLISHABLE_KEY}
# STRIPE_WEBHOOK_SECRET=${STRIPE_WEBHOOK_SECRET}

# Stripe Price IDs (defaults if not provided)
# STRIPE_PRICE_DISCOVER_MONTHLY=${STRIPE_PRICE_DISCOVER_MONTHLY}
# STRIPE_PRICE_DISCOVER_YEARLY=${STRIPE_PRICE_DISCOVER_YEARLY}
# STRIPE_PRICE_CONNECT_MONTHLY=${STRIPE_PRICE_CONNECT_MONTHLY}
# STRIPE_PRICE_CONNECT_YEARLY=${STRIPE_PRICE_CONNECT_YEARLY}
# STRIPE_PRICE_COMMUNITY_MONTHLY=${STRIPE_PRICE_COMMUNITY_MONTHLY}
# STRIPE_PRICE_COMMUNITY_YEARLY=${STRIPE_PRICE_COMMUNITY_YEARLY}

# JWT (inherited from root .env)
# JWT_SECRET=${JWT_SECRET}

# Service URLs (using Docker network names)
ORCHESTRATOR_URL=http://btd-orchestrator:9130
AUTH_SERVICE_URL=http://btd-auth:3005
USERS_SERVICE_URL=http://btd-users:3010
NOTIFICATION_SERVICE_URL=http://btd-notification:3006

# Internal API
INTERNAL_API_KEY=development-internal-key

# Logging
LOG_LEVEL=info